# HOME-001 WordPress Implementation Plan

> For agentic workers: use superpowers:executing-plans. Execute the authorized Gate8 scope continuously; existing dispatch authorizes ordinary technical decisions and implementation.

**Goal:** Deliver the approved homepage and shared chrome on an editable local WordPress site, with accurate Gate8 evidence for independent Gate9.

**Architecture:** One custom PHP theme renders fixed, escaped templates. A small first-party plugin owns typed, site-scoped editable content, authenticated admin forms, media references and validation. WordPress/MariaDB run in project-specific Docker volumes on loopback. No Next.js, GraphQL, analytics, or production writes.

**Tech Stack:** WordPress PHP 8.3, MariaDB 11.4, vanilla CSS/JS, WP-CLI, Node browser tests and PHP contract tests.

**Spec:** D:/23MySec/pages/home/06_handoff/HOME-001_D32_GATE6_HANDOFF_PACKAGE_V0.2.md, approved by its Gate6 closeout and Gate8 dispatch. Hashes and origin are in docs/handoffs/HOME-001-reception.md.

## Global Constraints

- site_scope=tio2-my; HOME-001 at /; no additional business pages.
- Full-page V1.1 with Hero V1.4 override; 1440/1024/768/390/320 screenshots; Hero 56/44/36px weight700, 2/2/3 lines at 1440/768/390.
- Preserve copy, URLs, 14 non-linked grades, five Schema nodes and seven relations. Local noindex, production canonical.
- Ordinary edits change database content, not theme code or approval IDs. No approval-content equality in visitor code.
- Cookie Settings no_optional_analytics; no analytics scripts, banner or fake consent storage.
- Keep source inputs read-only. Commit each independently verified task. Do not push, publish or merge without applicable authorization.

## Review Focus

1. Malformed/cross-site content: reject before atomic update, preserve previous content (Task1).
2. XSS/unsafe links and unrelated attachment IDs: reject saves and escape output (Task1/2).
3. Narrow viewport, keyboard and modal isolation: actual browser assertions and screenshots (Task3).
4. Content editing/restore and SEO parity: authenticate, edit representative fields and restore exact snapshot (Task4).
5. Candidate drift and missing link targets: content/artifact fingerprints, actual HTTP results and two preflights (Task5).

## Task 1: Editable content and isolated WordPress runtime

Files: compose.yaml, scripts/local.ps1, scripts/seed.php, wp-content/plugins/tio2-content/{tio2-content.php,includes/*,schema.json}, content/initial-home.json, tests/php/content-test.php, docs/source-map.json.

Interface: tio2_content(): array, tio2_field(string $key): string, tio2_validate_content(array $candidate): array|WP_Error. Fields are typed text, local path, or site-owned image. Content contains site_scope and schema_version, not approval IDs.

- [ ] Verify input hashes; map approved text/assets into seed and fixed field definitions.
- [ ] Red: php tests reject missing scope, empty required text, javascript URL, extra fields and invalid media without losing last saved values.
- [ ] Implement validation, capability/nonce protected grouped admin forms, media selector, seed and local environment; demonstrate database-backed fields.
- [ ] Green: PHP tests, PHP syntax, WordPress bootstrap/version checks; commit.

## Task 2: Server-rendered homepage and SEO

Files: wp-content/themes/tio2-malaysia/{functions.php,index.php,front-page.php,header.php,footer.php,inc/seo.php,template-parts/*,assets/*}, tests/http-contract.mjs.

Consumes typed fields from Task1; produces server HTML with one H1, one canonical, one JSON-LD graph and local noindex. Shared chrome assembled once. Assets mapped byte-for-byte; self-host Inter with license.

- [ ] Red: HTTP assertions for full approved text/link multiset, ordered modules, 14 unlinked grades, SEO graph and 404 dependencies.
- [ ] Implement PHP templates and approved CSS adaptation; no runtime prototype loading.
- [ ] Green: response tests and five-width rendering; commit.

## Task 3: Shared interactions and accessibility

Files: theme assets/site.js and CSS, tests/browser.spec.mjs.

Consumes shared modal markup; produces Menu and Cookie Settings with focus, Escape, inert background and scroll lock. Product details collapse on mobile; page RFQ display:none below768px.

- [ ] Red: keyboard open/close/return, focus containment, grade expansion, active nav and analytics absence tests.
- [ ] Implement native dialog based interactions with transparent menu backdrop, responsive reset and reduced motion.
- [ ] Green: browser tests, axe and visually inspect five-width screenshots plus menu/product/cookie states; commit.

## Task 4: Content editing and recovery

Files: tests/editor.spec.mjs, scripts/snapshot.php, docs/verification/home/*.

- [ ] Red: browser edit title/body/CTA/media/grade/SEO and see changed response without code changes; unauthorized and invalid saves rejected.
- [ ] Complete editor affordances as tests require; snapshot initial content and attachment identities.
- [ ] Restore approved content; compare exact snapshot, output and unchanged code hashes; commit verified evidence.

## Task 5: Review and Gate8 handoff

Files: scripts/artifact.py, docs/handoffs/HOME-001-gate8.md, docs/verification/home/gate8_evidence_manifest.json and AC/DEP evidence.

- [ ] Fresh independent code review, fix actionable findings and rerun affected tests.
- [ ] Commit implementation; create actual theme/plugin/asset artifact SHA and runtime marker, content snapshot and environment identity.
- [ ] Run original D23 validator and two-round preflight with WordPress mapping, require_build_marker=false and exact actual fingerprint.
- [ ] Commit receipt/evidence, generate final manifest bound to evidence HEAD; preserve runtime until Gate9 PASS/RETURN. Report physical AT/native zoom as NOT_VERIFIED if unavailable.
- [ ] Send complete result to D23 for independent Gate9; no production actions.

## Execution ledger

2026-09-20: Reception complete; 48/48 source hashes match. Empty repo has no historical baseline; first reception commit establishes baseline. Technical choices authorized by dispatch; no repeat design approval. Tasks execute inline. Runtime is in the user-named project; feature branch will isolate implementation history after initial baseline commit.
