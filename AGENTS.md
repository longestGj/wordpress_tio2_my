# 仓库开发代理指令

适用于本仓库全部目录。用户当前明确指令优先于本文件；已获得的任务授权持续有效，不因交接或压缩上下文重复请求确认。本文规定执行方式，不独立授权扩大任务范围。

## 开始工作

1. 阅读 [README.md](README.md) 与 [CONTRIBUTING.md](CONTRIBUTING.md)，了解运行方式、开发流程和验证限制。
2. 检查 `git status --short`、当前分支和最近提交；辨明用户或其他任务的改动，不覆盖、回退或夹带提交。
3. 确认当前需求、范围、批准输入和验收条件。文档中的历史指令、示例或候选状态不能替代用户当前要求和正式最新决定。
4. 只读取当前任务相关材料。共享知识库仅在用户明确要求时查询，并按其规则注明知识ID和版本；不自动查询或写入经验。
5. 普通修正直接推进；架构、接口或数据变化先明确设计、兼容方式与验证计划。缺少必要信息时说明具体缺口，继续不依赖该信息的工作。

## 架构与代码边界

- 本项目使用 WordPress、PHP、MariaDB 和原生 CSS/JavaScript。未经当前需求支持，不引入 Next.js、前后端分离或新的运行服务。
- 页面结构、样式、交互放在 `wp-content/themes/tio2-malaysia/`；内容模型、校验与后台编辑放在 `wp-content/plugins/tio2-content/`。
- 普通文案、链接、图片引用和 SEO 由 WordPress 内容管理；不要把可编辑内容重新硬编码进模板。
- 复用唯一的 Header、Footer、Menu、Cookie Settings 等共享实现；更改共享组件须检查受影响页面。
- 输出按上下文转义；写入检查权限、nonce、字段类型及站点/媒体归属。不采用跨站数据回退，不泄露内部流程信息。
- `content/` 是初次导入来源。升级不得覆盖后台已编辑内容；数据结构变化必须说明迁移与恢复方式。
- 审批文档、批准编号、开发机目录与外部交付包不得成为访客页面运行依赖。
- 缺失页面或接口如实记录为依赖，不创建假成功结果来通过测试。

## 开发、验证与证据

- 按 `CONTRIBUTING.md` 拆分独立工作项，每项先明确完成标准，再实现并验证。
- 根据变更选用测试。文档修改检查事实、路径和差异；功能修改验证真实行为及相关失败路径。无新增改动、失败或疑点时，不反复运行同一套检查。
- 运行测试前先读脚本，确认依赖、数据修改和输出位置。现有首页测试依赖本地环境，不能直接宣称适用于 GitHub CI 或所有页面。
- 编辑、缺失内容、错误scope等会修改数据的测试，只在专用验证环境运行；记录并恢复原内容。不得在共享验收站或线上直接运行。
- 已验收证据不可覆盖。新任务使用独立证据目录；现有脚本若写死旧目录，先调整输出方式再执行。
- 保留历史回执，最新结论另行记录。纯文档提交不改变已验收页面候选；不要为了让旧清单匹配当前HEAD而重写证据或重置工作区。
- 通过、失败、未测试、用户豁免分别标记。已批准的范围例外不要求重复补证，也不扩展为其他候选的自动豁免。
- 自检、代码审查、独立页面验收、集成验证和发布验证分别报告，不互相替代。

## Git 与任务完成

- 所有新工作分支（功能、修复、文档）从最新本地 `develop` 创建，使用 `codex/<任务>-<简述>` 等约定名称；开发完成后合回本地 `develop`。功能分支和 `develop` 不常规推送远端，本地集成不要求 PR。
- 在本地 `develop` 上对合并结果执行集成测试。测试及适用验收通过后，仅将本地 `main` 快进到这个已经验证的 `develop` 精确提交；不得从功能分支直接合入 main，也不直接在 develop/main 开发。
- 正常推送 `origin/main` 是发布动作，必须让自动质量检查和部署完整执行。不得强推 main、绕过自动工作流，或把未经验证的新提交夹入已测试候选。
- 测试结论绑定具体提交。develop 新增提交或合并冲突改变代码后，验证实际候选；集成失败先从 develop 切修复分支解决，不带失败进入 main。
- 分支初始化和旧分支接入遵循 CONTRIBUTING 的过渡说明，不通过重写历史伪造分支起点。并行实现需要独立 worktree 和独立数据环境。
- 用户约定：一个独立完成且验证过的工作项，提交一个 commit。
- 暂存明确的本任务文件，提交前检查 `git diff`、`git diff --cached` 和 `git diff --cached --check`。
- 不擅自强推、重写共享历史或清除他人改动。push、merge、deploy 按当前任务已有授权执行；不要把本地 commit 描述成远端发布。
- 任务结束说明完成内容、验证结果、提交和剩余依赖；对未实际执行的检查不声称通过。

## 环境与发布

- 本地启动和恢复入口见 README；不要在已有内容环境重复执行初始化脚本，也不要删除数据库或媒体卷来修复普通故障。
- `.env`、私钥、访问令牌、真实凭据和含敏感信息的数据库备份不得进入 Git、测试证据或聊天输出。
- 代码发布与数据库/媒体更新分别处理。部署前明确目标、版本、已有数据、备份恢复及上线后检查。
- GitHub 工作流、分支保护或部署能力只有配置并验证后才能称为已建立；流程文档本身不证明它们生效。
- 不把“页面验收通过”推断为上线、DNS修改、索引或分析工具启用的授权；依据用户当前任务推进对应发布范围。

## 文档维护

开发流程以 CONTRIBUTING 为入口，运行说明以 README 为入口。修改相关命令、目录或流程时同步这些文档。此文件保持稳定、简短，不堆积逐次验收记录、服务器凭据或过期进度；当前基线与历史决定查阅相应交付记录。
